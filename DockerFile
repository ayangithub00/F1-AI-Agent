FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY f1/ .

ENV DJANGO_SETTINGS_MODULE=f1.settings

EXPOSE 7860

CMD ["gunicorn", "f1.wsgi", "--bind", "0.0.0.0:7860", "--timeout", "120"]